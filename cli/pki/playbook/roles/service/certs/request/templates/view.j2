#!/bin/bash
export TARGET=$1
if [[ $TARGET == "csr" ]]; then
    openssl req \
        -in {{ blueprint }}.csr \
        -noout \
        -text
elif [[ $TARGET == "crt" ]]; then
    openssl x509 \
        -in {{ blueprint }}.crt \
        -noout \
        -text
fi