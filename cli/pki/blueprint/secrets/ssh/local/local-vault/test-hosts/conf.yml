ssh_secrets_config:
  path: "test-hosts-ssh-client-signer/"
  generate_signing_key: true
  vault_roles:
    - role_name: developer
      role_options: "{{ lookup('file','{{ role_options_dir }}/developer.json') }}"