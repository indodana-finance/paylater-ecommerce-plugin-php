#!/usr/bin/env bash
set -e 
trap "exit" INT

source pki/app/.helper/common

echo ""
echo -e "Revoke certificate(s) of a particular service." \
        "Unless you are revoking for a localhost, you need admin access" \
        "to revoke a certificate." | fold -w 80 -s
echo ""

pkictl_run "service.certs.revoke" --help